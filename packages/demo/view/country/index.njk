{% extends "../layout/default.njk" %}

{% block content %}
    {% from "../component/data-table.njk" import dataTable %}
    {{ dataTable(data) }}

    {% from "../component/pagination.njk" import pagination %}
    {{ pagination($pagination) }}
{% endblock %}
