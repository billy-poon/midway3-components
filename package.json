{
    "name": "midway3-components",
    "private": true,
    "scripts": {
        "dev": "pnpm demo dev",
        "dev:cli": "pnpm demo dev:cli",
        "demo": "pnpm -F @midway3-components/demo",
        "demo:build": "pnpm demo build",

        "cli": "pnpm -F @midway3-components/cli",
        "cli:build": "pnpm cli build",
        "cli:publish": "pnpm cli build && pnpm cli publish --access public",

        "core": "pnpm -F @midway3-components/core",
        "core:build": "pnpm core build",
        "core:publish": "pnpm core build && pnpm core publish --access public",

        "web": "pnpm -F @midway3-components/web",
        "web:build": "pnpm web build",
        "web:publish": "pnpm web build && pnpm web publish --access public",

        "drizzle": "pnpm -F @midway3-components/drizzle",
        "drizzle:build": "pnpm drizzle build",
        "drizzle:publish": "pnpm drizzle build && pnpm drizzle publish --access public",

        "build": "pnpm core:build && pnpm web:build && pnpm drizzle:build && pnpm cli:build && pnpm demo:build",
        "test": "echo \"Error: no test specified\" && exit 1"
    },
    "devDependencies": {
        "mwts": "^1.3.0"
    }
}
